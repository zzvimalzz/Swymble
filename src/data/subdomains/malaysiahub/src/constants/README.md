# src/constants

True domain constants — facts of Malaysia and of this product that are the
same in every environment: state/district code tables, module identifiers,
data tier labels.

If a value could ever change between dev and production, it is configuration
(`src/config`), not a constant.
