/**
 * External API endpoints (realtime feeds consumed client-side).
 * Static datasets come from the data lake via src/services/artifact-client;
 * these are the official public APIs that serve live data with open CORS.
 */
export const DATA_GOV_MY_API_BASE = "https://api.data.gov.my";
